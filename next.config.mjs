/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.jsdelivr.net",
      "loremflickr.com",
      "source.unsplash.com",
      "picsum.photos",
      "avatars.githubusercontent.com",
    ],
  },
};

export default nextConfig;
